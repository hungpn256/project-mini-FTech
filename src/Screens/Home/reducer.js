const initialState = {
  user: {},
};
import {USER_SET} from './constants';
const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default reducer;
